@* Not Injected! *@
@* @model IEnumerable<Product> *@
@* *@
@* @foreach (var product in Model) *@
@* { *@
@*     @await Html.PartialAsync("Partials/ProductList/_ProductListItem", product) *@
@* } *@

@* Injected! *@
@*  This will ONLY work if you first inject the ProductService! 
    It's added to the view imports file. *@
@foreach (var product in productService.GetProducts())
{
    @await Html.PartialAsync("Partials/ProductList/_ProductListItem", product)
}